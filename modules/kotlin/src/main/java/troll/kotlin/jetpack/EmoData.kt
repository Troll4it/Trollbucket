package troll.kotlin.jetpack/** * author : TangPeng * date : 4/12/22 16:41 * description : */class Emo(var age: Int) {    val a        get() = age >= 16    val b = age >= 18}//data class EmoData(val name: String, val age: Int)fun main() {    val emoData = EmoData(name = "Emo", age = 1)    val (name, age) = emoData    println("析构声明：name == $name | age == $age")    val mapOf = mapOf(1 to 2, 3 to 4)    for ((key, value) in mapOf) {        println(" key值 $key value值$value")    }}