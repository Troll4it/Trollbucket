package troll.kotlin.other;import androidx.annotation.NonNull;import java.util.Arrays;/** * author : TangPeng * date : 4/16/22 18:00 * description : */public class School implements Cloneable {    String name;    Student student;    @NonNull    @Override    protected Object clone() throws CloneNotSupportedException {//        School school = new School(name, new Student());   是否写这行代码，如果有就是对内部的深拷贝        return super.clone();    }}class Student implements Cloneable {    String age;    public Student(String age) {        this.age = age;    }    @NonNull    @Override    protected Object clone() throws CloneNotSupportedException {        return super.clone();    }}class Test {    public static void main(String[] args) {        Student student = new Student("11");        try {            Student clone = (Student)student.clone();            System.out.println("args = " + student +"------"+clone);        } catch (CloneNotSupportedException e) {            e.printStackTrace();        }    }}