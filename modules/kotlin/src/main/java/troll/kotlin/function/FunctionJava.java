package troll.kotlin.function;import android.view.View;import java.math.BigDecimal;/** * author : TangPeng * date : 3/17/22 20:37 * description : */public class FunctionJava {    public static void main(String[] args) {//        View view = new View();//        view.setOnClickListener(new View.OnClickListener() {//            @Override//            public void onClick(View view) {////            }//        });////        view.setOnClickListener(view1 -> test());        double a = 5000;        double b = 69000  ;        double total = 0;        for (int i = 1; i < 20; i++) {            total += a;            double per = 1;            double o = 0.95;            if (i == 0 || i == 1) {                per = 1;                o = 0.92;            } else {                o = 0.92;                per = 1.1;            }            a = per * a;            b = o * b;            System.out.println("下跌 " + i + "次，净值 " + BigDecimal.valueOf(b).setScale(4, BigDecimal.ROUND_DOWN) + "  买入  " + BigDecimal.valueOf(a).setScale(4, BigDecimal.ROUND_DOWN) + "  总投入 " + BigDecimal.valueOf(total).setScale(4, BigDecimal.ROUND_DOWN));        }    }    private OnClickListener onClickListener;    private OnContextClickListener contextClickListener;    public void setOnClickListener(OnClickListener l) {        this.onClickListener = l;    }    public void setOnContextClickListener(OnContextClickListener l) {        this.contextClickListener = l;    }    interface OnContextClickListener {        boolean onContextClick(View v);    }    interface OnClickListener {        void onClick(View view);    }}// 如何利用高阶函数来优化// kotlin 语言设计： 用函数类型替代接口定义     用Lambda表达式作为函数参数