package com.troll.design.responsibilitychain.linkabstract class Handler {    var successor: Handler? = null    public fun setHandler(successor: Handler) {        this.successor = successor    }    public fun handle(): Boolean {        val boolean = false        if (boolean.not() && successor != null) {            successor?.handle()        }        return boolean    }    protected abstract fun doHandle(): Boolean}